# GAPI 代碼檢視報告（第一輪）

**檢視日期**：2026-03-01  
**檢視者**：光團 (Guang1-5)  
**原則**：只記錄問題，不改代碼

---

## P0 - API 規格與認證（光一）

### 1. API 規格定義問題
| 問題 | 說明 |
|------|------|
| 錯誤碼定義不一致 | `API_SPEC.md` 定義 `expired_token`，但 `mcp_server.py` 未區分過期 vs 無效 |
| HTTP/WebSocket 訊息類型命名不一致 | 文檔用 `api_key`，實際用 `token` 參數 |
| 缺少 `rate_limit` 錯誤處理 | 定義了但未實作 |

### 2. WebSocket 認證邏輯問題 🔴
| 問題 | 嚴重程度 |
|------|----------|
| **前端 Token 簽名與 Server 驗證完全不匹配** | 🔴 P0 安全漏洞 |
| Server 端簽名驗證邏輯與前端不匹配 | 🔴 |
| 開發模式硬編碼 Secret Key | 🔴 |
| Token 過期檢查時區問題 | ⚠️ |
| WebSocket 認證超時處理不完整 | ⚠️ |

### 3. Session 管理問題
| 問題 | 說明 |
|------|------|
| Session 清理機制缺失 | `disconnect()` 未調用 `store.delete_session()` |
| Extension ID 重複連接處理 | 舊 session 沒有被清理 |

### 4. 其他問題
- CORS 設定過寬 (`allow_origins = ["*"]`)
- 錯誤訊息暴露內部細節
- 缺少請求日誌

---

## P1 - 數據提取與儲存（光二）

### 檔案路徑問題
- 在 `projects/GAPI` 目錄中未找到 `content.js` 和 `background.js`
- 懷疑實際路徑可能在 Extension 專案中

### Server 儲存路由問題
- 缺乏實際的儲存路由實作
- 只處理了認證，儲存機制未完成

---

## P2 - Admin Web 與 DOM 監聽器（光三）

### 1. 記憶體洩漏風險 🔴
| 問題 | 說明 |
|------|------|
| MutationObserver 未正確清理 | 缺乏統一的清理機制 |
| 重複設置觀察器 | 沒有檢查機制避免重複 |
| 觀察器管理器使用不一致 | 部分用 manager，部分直接創建 |
| 缺乏全局清理機制 | 沒有 `beforeunload`/`pagehide` 清理 |
| 局部觀察器未設置超時 | 長期運行的觀察器可能累積 |

### 2. 其他問題
- console.log 過多
- 錯誤處理重複

---

## P3.1 - 圖片上傳服務（光四）

### 1. 安全問題 🔴
| 問題 | 說明 |
|------|------|
| AUTH_SECRET 硬編碼 | 開發用密鑰寫死 |
| CORS 配置過於寬鬆 | `allow_origins=["*"]` |
| 開發模式允許無認證訪問 | 生產環境風險 |

### 2. 圖片上傳邏輯問題
| 問題 | 說明 |
|------|------|
| MIME 類型推斷不嚴謹 | 未知類型預設為 `.png` |
| 缺少檔案大小限制 | 可能導致儲存空間濫用 |
| 缺少圖片驗證 | 未驗證有效性格式 |
| URL 路徑暴露內部結構 | 暴露儲存路徑 |

### 3. 程式碼重複問題
- `image_upload.py` 與 `mcp_server.py` 函數完全重複

### 4. background.js 問題
- Token 生成邏輯與 Server 不匹配
- 重連機制缺乏退避策略
- 內存洩漏風險

---

## P3.2 - API Key 認證（光五）

### 1. 安全問題 🔴
| 問題 | 說明 |
|------|------|
| API Key 格式安全性不足 | signature 僅 24 字元 |
| API Key 驗證邏輯缺陷 | 未驗證 signature 有效性 |
| API Key 明文儲存 | 資料庫中以明文存放 |
| API Key 洩漏風險 | HTTP 回傳中明文傳輸 |

### 2. 權限問題
| 問題 | 說明 |
|------|------|
| 管理員權限檢查缺失 | 普通 Key 持有者可管理其他 Key |
| API Key 名稱注入風險 | 輸入未驗證 |

### 3. 其他問題
- WebSocket 缺乏 API Key 支援
- 靜態 Secret Key 風險
- 缺乏 API Key 使用統計
- Token 生成演算法可預測

---

## 優先級總結

| 等級 | 問題 | 數量 |
|------|------|------|
| 🔴 P0 | Token 簽名不匹配（認證機制失效） | 1 |
| 🔴 P0 | AUTH_SECRET 硬編碼 | 1 |
| 🔴 P0 | API Key 明文儲存 | 1 |
| 🟠 P1 | MutationObserver 未正確清理 | 1 |
| 🟠 P1 | Session 清理機制缺失 | 1 |
| 🟡 P2 | CORS 過寬 | 3 |
| 🟡 P2 | 程式碼重複 | 2 |

---

## 建議優先處理順序

1. **修正前端 Token 簽名生成邏輯**（P0 安全漏洞）
2. **實作 Session 清理機制**
3. **將 AUTH_SECRET 移至環境變數**
4. **修正 MutationObserver 清理邏輯**
5. **統一錯誤碼定義**
6. **收緊 CORS 設定**

---

*報告完成時間：2026-03-01 19:19 GMT+8*
